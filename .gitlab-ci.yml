image: microsoft/dotnet:latest

build:
    stage: build
    script: 
        - 'dotnet build --no-restore'

test:
    stage: test
    script: 
        - 'dotnet test --no-restore'

deploy:
    stage: deploy
    environment: production

